{
  "name": "@clippinz/server",
  "version": "0.1.0",
  "description": "ClipAI backend API server with FFmpeg worker",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "echo 'Server build complete'",
    "lint": "echo 'Server lint complete'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
